import { Router } from 'express';
const router = Router();

// Placeholder routes for future Salla integrations
router.get('/products', async (req, res) => {
  res.json({ data: [] });
});

export default router;
